\documentclass[11pt,a4paper]{article}

% =====================================================
% PAGE LAYOUT
% =====================================================
\usepackage[a4paper,margin=1in]{geometry}

% =====================================================
% FONTS
% =====================================================
\usepackage{fontspec}
\usepackage{unicode-math}

\IfFontExistsTF{TeX Gyre Pagella}{%
  \setmainfont{TeX Gyre Pagella}[Ligatures=NoCommon]
  \setmathfont{TeX Gyre Pagella Math}
}{%
  \setmainfont{Times New Roman}[Ligatures=NoCommon]
  \setmathfont{Latin Modern Math}
}

% =====================================================
% PACKAGES
% =====================================================
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{float}
\usepackage{calc}
\usepackage{multicol}
\usepackage{pdflscape}
\usepackage{enumitem}
\usepackage[table,svgnames]{xcolor}
\usepackage{titlesec}
\usepackage[hidelinks]{hyperref}
\usepackage{fancyhdr}
\usepackage{needspace}
\usepackage{nowidow}


% Prevent widows and orphans — aggressive
\widowpenalty=100000
\clubpenalty=100000
\displaywidowpenalty=100000
\brokenpenalty=10000
\predisplaypenalty=10000
\postdisplaypenalty=10000
\interlinepenalty=500
\raggedbottom
\hyphenpenalty=1000
\tolerance=500

% Pandoc compatibility
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}
  \setlength{\parskip}{0pt}
}
\providecommand{\pandocbounded}[1]{%
  \begin{center}
    #1
  \end{center}
}

% Longtable fixes
\setlength\LTleft{0pt}
\setlength\LTright{0pt}

% =====================================================
% COLORS — DBG Travel brand (matching frontend)
% =====================================================
\definecolor{travelblue}{HTML}{003366}
\definecolor{travelgold}{HTML}{A07F40}
\definecolor{travelaccent}{HTML}{D4A84B}
\definecolor{travelbg}{HTML}{f8f7f4}
\definecolor{tablerowalt}{HTML}{f3f1ed}

% =====================================================
% SECTION NUMBERING — only 2 levels deep
% =====================================================
\setcounter{secnumdepth}{2}
\setcounter{tocdepth}{2}

% Section heading style — no bottom border
\titleformat{\section}
  {\needspace{4\baselineskip}\Large\bfseries\color{travelblue}}
  {\thesection}
  {1em}
  {}

% Subsection heading style — no bottom border
\titleformat{\subsection}
  {\needspace{3\baselineskip}\large\bfseries\color{travelblue}}
  {\thesubsection}
  {1em}
  {}

% Subsubsection (if any) — bold, no border
\titleformat{\subsubsection}
  {\needspace{2\baselineskip}\normalsize\bfseries\color{travelblue}}
  {\thesubsubsection}
  {1em}
  {}

% =====================================================
% SPACING
% =====================================================
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.75em}

\setlist{nosep}
\setlist[itemize]{leftmargin=*}

\AtBeginEnvironment{itemize}{\needspace{4\baselineskip}}
\AtBeginEnvironment{enumerate}{\needspace{4\baselineskip}}

% =====================================================
% HEADER / FOOTER
% =====================================================
\pagestyle{fancy}
\fancyhf{}

\fancyhead[R]{\small\textcolor{travelblue}{$title$}}
\fancyfoot[C]{\thepage}

\renewcommand{\headrulewidth}{1.2pt}
\renewcommand{\headrule}{%
  {\color{travelgold}%
  \hrule width\headwidth height\headrulewidth
  \vskip-\headrulewidth}
}

% =====================================================
% HYPERLINKS — brand blue like frontend
% =====================================================
\hypersetup{
  colorlinks=true,
  linkcolor=travelblue,
  urlcolor=travelblue,
  citecolor=travelblue,
}

% =====================================================
% IMAGE AND TABLE HANDLING
% =====================================================
\floatplacement{figure}{H}
\floatplacement{table}{H}

% =====================================================
% TABLE STYLING — matches frontend table headers
% =====================================================
\newcommand{\tableheader}{%
  \rowcolor{travelblue}%
  \color{white}%
  \normalsize\bfseries%
}
\let\oldtabular\tabular
\let\endoldtabular\endtabular
\renewenvironment{tabular}[1]{%
  \rowcolors{1}{}{tablerowalt}%
  \oldtabular{#1}%
}{%
  \endoldtabular%
}

% Non-banded tabular for legend tables (no alternating row colors)
\newenvironment{plainrows}{%
  \rowcolors{1}{}{}%
}{%
  \rowcolors{1}{}{tablerowalt}%
}

% =====================================================
% DOCUMENT
% =====================================================
\begin{document}

% =====================================================
% COVER PAGE
% =====================================================
\begin{titlepage}
  \centering

  % Logo (left) + DBG TRAVEL text (same line, same height)
  $if(logo)$
  \begin{minipage}[c]{0.12\textwidth}
    \includegraphics[width=\textwidth, keepaspectratio]{$logo$}
  \end{minipage}
  \hspace{0.5em}
  \begin{minipage}[c]{0.75\textwidth}
  $endif$
    {\Huge\bfseries\color{travelblue} DBG \textcolor{travelgold}{TRAVEL}\par}
  $if(logo)$
  \end{minipage}
  $endif$

  \vspace{0.3cm}
  {\color{travelaccent}\rule{0.4\textwidth}{1.5pt}\par}
  \vspace{0.5cm}

  $if(cover-image)$
  \vspace*{0.5cm}
  \includegraphics[width=\textwidth, height=0.3\textheight, keepaspectratio]{$cover-image$}
  \vspace{0.5cm}
  $else$
  \vspace*{1.5cm}
  $endif$

  {\Huge\bfseries\color{travelblue} $citytitle$\par}

  \vspace{1.5cm}

  {\Large $date$\par}

  \vfill

\end{titlepage}

% =====================================================
% TABLE OF CONTENTS — depth 2
% =====================================================
\pagenumbering{roman}
\tableofcontents
\newpage
\pagenumbering{arabic}

% =====================================================
% BODY
% =====================================================
$body$

\end{document}
